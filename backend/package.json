{
  "name": "petadote-backend",
  "version": "1.0.0",
  "description": "Backend simples do projeto PetAdote",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "c8 --reporter=lcov --reporter=text node --test"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "c8": "^11.0.0"
  }
}
